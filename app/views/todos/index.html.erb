<%= link_to "Add a new todo", new_todo_path %>

<ul class="todos">
  <% @todo.each do |todo| %>

    <li>
      <%= todo.title %>
      <%= button_to "Mark complete", todo_completion_path(todo) %>
    </li>
  <% end %>
</ul>
